/**
 * Web 端常量 - 从共享配置导出
 */
export { LANGUAGES, DEFAULTS, PRESET_MODELS, PROVIDER_LABELS, PROVIDER_DEFAULT_BASE_URLS } from './config/defaults';

export const CONTEXT_WINDOW_CHAR_LIMIT = 3500000; // ~875k tokens
